import React, { Component } from 'react';
import * as firebase from 'firebase';
import './App.css';
import {hashHistory,Link} from 'react-router';

export default class Signout extends Component{
    render(){
        return <p>SignedOut</p>
    }
}